# Simple workflow for reference
name: CD-Deploy-container

on: 
  workflow_run:
    workflows: ['CI-build-and-push-images']
    types:
      - completed
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
jobs:
  deploy_images:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Copying Docker compose
        working-directory: "devops"
        run: |
            ls -al
            docker-dir="~/docker-compose/"
            mkdir -p $docker-dir
            cp docker-compose.yml $docker-dir
      
      - name: deploying-comtainer
        run: |
            cd $docker-dir
            docker compose up -d
